function setup() {
    createCanvas(width, height);
}
function draw() {
    background(230);
    tablero();
    L1.draw();
    l2.draw();
    I.draw();
    Cuad.draw();
    S.draw();
    Z.draw();
    T.draw();
}

function keyPressed() {
    if (keyCode == RIGHT_ARROW) {
        
    }
}